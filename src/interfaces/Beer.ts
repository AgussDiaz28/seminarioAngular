export class Beer {
  // equals(beer: Beer) : boolean {
  //   return this.name == beer.name;
  // }
  name: string;
  description: string;
  img: string;
  price: number;
  inStock: boolean;
  stockLeft: number;
  onSale: boolean;
}
